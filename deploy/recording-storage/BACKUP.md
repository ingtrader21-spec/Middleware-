# Backup

Back up versioned objects and bucket configuration to an independently protected,
private target. The backup auditor identity may list versions and lock settings
but cannot read or delete recordings. Quarterly restore rehearsals must verify
checksums, version IDs, retention and legal holds without using production data.
