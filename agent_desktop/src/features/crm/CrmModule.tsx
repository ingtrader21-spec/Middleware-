import {Card} from "../../components/Card"; import {Badge} from "../../components/Badge"; import type {Lead,CallRecord} from "../../types/desktop";
export function CrmModule({lead,history}:{lead:Lead;history:CallRecord[]}){return <Card title="CRM" action={<Badge tone="info">Mock Odoo</Badge>}><div className="identity"><div className="avatar">JS</div><div><strong>{lead.name}</strong><p>{lead.company}</p></div></div><dl className="facts"><div><dt>Phone</dt><dd>{lead.phoneMasked}</dd></div><div><dt>Email</dt><dd>{lead.email}</dd></div><div><dt>Source</dt><dd>{lead.source}</dd></div><div><dt>Timezone</dt><dd>{lead.timezone}</dd></div></dl><h3>Call history</h3><div className="history">{history.map(x=><div key={x.id}><span><strong>{x.result}</strong><small>{x.when} · {x.agent}</small></span><b>{x.duration}</b></div>)}</div></Card>}
