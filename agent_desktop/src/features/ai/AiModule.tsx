import {Card} from "../../components/Card"; import {Badge} from "../../components/Badge"; import type {AiInsight} from "../../types/desktop";
export function AiModule({ai}:{ai:AiInsight}){return <Card title="AI Assistant" action={<Badge tone="warn">Advisory mock</Badge>}><div className="ai-score"><span>Sentiment <b>{ai.sentiment}</b></span><span>Confidence <b>{ai.confidence}%</b></span></div><div className="recommend"><small>Suggested next question</small><strong>{ai.nextQuestion}</strong></div><p><b>Objection:</b> {ai.objection}</p><p className="warning-text"><b>Disclosure:</b> {ai.disclosure}</p><div className="transcript">{ai.transcript.map((x,i)=><p key={i}>{x}</p>)}</div><details><summary>Draft call summary</summary><p>{ai.summary}</p></details></Card>}
