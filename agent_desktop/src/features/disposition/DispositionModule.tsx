import {useState} from "react";import {Card} from "../../components/Card";
export function DispositionModule({onSave}:{onSave:(v:string)=>Promise<void>}){const [value,setValue]=useState("");return <Card title="Disposition"><label className="field">Outcome<select value={value} onChange={e=>setValue(e.target.value)}><option value="">Select an outcome</option><option>Qualified</option><option>Callback requested</option><option>Not interested</option><option>Wrong number</option><option>Do not call</option></select></label><button disabled={!value} onClick={()=>void onSave(value)}>Save mock disposition</button></Card>}
