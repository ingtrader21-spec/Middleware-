# Qwen authentication verifier DER VEX

This directory records the final independent Security Owner decision for the
Linux/amd64 verifier image
`sha256:cc7e2457fdd69fdd1bf766831f8f86396495b47e377e86b2d9df1d4fb5432390`.

The authenticated decision is GitHub review
`pullrequestreview-4859576544`, submitted by `kazan555` (user ID `77101516`).
It applies only to the exact image and SBOM recorded in the decision file.
The record is unsigned until the protected publication workflow produces the
final VEX attestation.
