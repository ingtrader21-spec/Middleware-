# Gateway schema-aware acknowledgement RC2 security binding

This directory binds the reviewed schema-aware acknowledgement gateway source,
SBOMs, scanner evidence, security-owner decision, and OpenVEX assertions to the
exact immutable registry digest.

The authorization boundary covers signing and release verification only.
Deployment and the bounded production canary remain separate fail-closed
transactions.
