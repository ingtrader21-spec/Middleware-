# Gateway acknowledgement forensics RC3

This package binds the reviewed forensic-response correction, immutable gateway
image, SBOMs, scanner results, fixed-backport decision, and OpenVEX statements.

The image remains fail-closed by default. Deployment and bounded-canary
execution are separate post-signing operations.
