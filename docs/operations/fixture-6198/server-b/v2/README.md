# Recovered fixture 6198 Server B package v2

Authority is the checksum-authenticated historical source recovered on Server B.
Its unsafe historical external-route activation script is evidence-only and is
not reused. This package permits only `10.40.0.1/32` to reach internal Echo
destination `*43`. Scripts default to dry-run. `--execute` requires a separate
mission, a validated change ID, and the reviewed package checksum. No secret is
stored here; rendering reads a protected mode-0600 file.

The AOR section is named `6198` because Asterisk registrar lookup maps the
registration address-of-record user directly to that section name.
