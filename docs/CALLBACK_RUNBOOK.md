# Callback Operations Runbook

Monitor due/completed/missed/overdue counts, reminder failures, popup failures, scheduler heartbeat and reconciliation findings. Pause delivery workers without modifying callback records when n8n, email, WebSocket or Odoo is unavailable. After recovery, run reconciliation; version and delivery uniqueness prevent obsolete or duplicate notifications.
