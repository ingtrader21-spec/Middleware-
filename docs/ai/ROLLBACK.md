# Rollback

Stop new claims first. Allow leased jobs to finish or expire. Roll back the Middleware image while retaining the forward-compatible schema and accepted jobs. Disable the worker by removing the separately approved activation marker and stopping its service. Restore the prior model registry file. Do not delete jobs, results, approvals, usage, or audit events. Database downgrade is permitted only in disposable rehearsal with no orchestration rows.
