# 05 — Signed Middleware Release run 35602170321 (exact main `8ecf6e9`)

https://github.com/ingtrader21-spec/Middleware-/actions/runs/35602170321

## Trigger and gate

`.github/workflows/release.yml` on `8ecf6e9`:

- `on: workflow_run: workflows: ["Middleware CI"], types: [completed], branches: [main]`
- job `release` (the single forward production publisher, narrow-mutation authorized by the protected validator) runs only when `github.repository == 'ingtrader21-spec/Middleware-'` and the triggering run concluded `success` on `main` from the same repository.
- `IMAGE_REPOSITORY: ghcr.io/ingtrader21-spec/codestra-middleware`
- `SOURCE_SHA: github.event.workflow_run.head_sha` = `8ecf6e9b75a7adf4c82c4c7094ba71d64d73e692`
- `EXPECTED_SCHEMA_HEAD: 0067_service_catalog_monitoring_state`
- permissions: `contents: read`, `packages: write`, `id-token: write`

The PAS-27 authority change is therefore live on protected main: the release publisher targets the canonical forward package, not the pre-transfer `appolon1908-hue` namespace.

## Step results

| Step | Result |
| --- | --- |
| Check out the exact CI-accepted source | ok (`HEAD is now at 8ecf6e9`) |
| Prove release source and initialize immutable metadata | ok (`RELEASE_SOURCE_SHA=8ecf6e9b…`) |
| Set up Docker Buildx | ok |
| Authenticate to GHCR (`docker/login-action`, registry `ghcr.io`, password `GITHUB_TOKEN`) | `Login Succeeded!` |
| Build and publish one provenance-bearing image | **FAIL** at push |
| SBOM / Grype / Trivy / Cosign sign / SLSA attest / verify | not reached |
| Recover provenance for the pinned signed release (`attest-pinned-release`) | skipped (`if: false` by design) |

The Docker build itself completed (base `python:3.14.7-slim-bookworm@sha256:82bc3c53…`, pinned dependencies installed, image assembled). The failure is the registry push:

```
#26 ERROR: failed to push ghcr.io/ingtrader21-spec/codestra-middleware:sha-8ecf6e9b75a7adf4c82c4c7094ba71d64d73e692-run-35602170321-attempt-1: denied: permission_denied: read_package
ERROR: failed to build: failed to solve: failed to push ghcr.io/ingtrader21-spec/codestra-middleware:sha-8ecf6e9b75a7adf4c82c4c7094ba71d64d73e692-run-35602170321-attempt-1: denied: permission_denied: read_package
##[error]buildx failed with: ERROR: failed to build: failed to solve: failed to push ghcr.io/ingtrader21-spec/codestra-middleware:sha-8ecf6e9b75a7adf4c82c4c7094ba71d64d73e692-run-35602170321-attempt-1: denied: permission_denied: read_package
```

Note the login step's `username: kazan555`: `GITHUB_ACTOR` for a `workflow_run` chain that started from an auto-merge is the user whose review satisfied the merge. With a `GITHUB_TOKEN` password the username is not the authorization principal; the principal is the GitHub Actions installation token scoped to `ingtrader21-spec/Middleware-`.

## Comparison with earlier release attempts

| Run | Main | Target | Failure |
| --- | --- | --- | --- |
| 35528661211 | `2862af0a` | `ghcr.io/appolon1908-hue/codestra-middleware` | `permission_denied: The requested installation does not exist.` |
| 35553816393 | `4f8069e7` | `ghcr.io/appolon1908-hue/codestra-middleware` | same |
| 35594050666 | `bd406a65` | `ghcr.io/appolon1908-hue/codestra-middleware` | same |
| **35602170321** | **`8ecf6e9b`** | **`ghcr.io/ingtrader21-spec/codestra-middleware`** | **`permission_denied: read_package`** |

The namespace failure is resolved; the remaining denial is a package-level access check on the new owner's package (06).

No artifact was published; no digest exists; `RELEASE_SEAL=OPEN`.
