# Cross-repo decision record (V3 command kernel)

Middleware freezes its own authority first; nothing in sibling repositories is modified by this PR.

| Open item (from the lane-D/F audit) | Classification | Decision / note |
|---|---|---|
| n8n-crm-automation vs n8n-automation client id | N8N_OWNER_DECISION | The kernel authorizes any azp registered in config/control-plane-callers.v1.json; `n8n-automation` is registered today (with its connector authority). If N8N adopts `n8n-crm-automation`, Keycloak issues it and Middleware adds the caller row — no kernel change. |
| 4 missing Keycloak clients (callback-ui, github-app, n8n-operations-automation, observability-collector) | KEYCLOAK_OWNER_DECISION | Not required by the six kernel routes. The kernel's workload callers are middleware-api (synthetic only), odoo-integration, n8n-automation, middleware-worker (reads). |
| 30 unissued Keycloak scopes | KEYCLOAK_OWNER_DECISION (3 DECIDED_BY_V3_CONTRACT) | V3 defines exactly platform.command, platform.command.read, platform.command.replay (+ metrics.read for /metrics) and the realm role platform-operator for replay; the rest are pre-existing domain scopes outside this contract. |
| 7 Odoo calls outside the current edge contract | ODOO_OWNER_DECISION | The Odoo integration's outbound writes into Middleware go through POST /platform/v1/commands (crm.* / email.* / sms.* on the odoo-integration caller) or the existing ingress routes; Odoo decides which legacy control-plane routes it retires. Middleware→Odoo effects run only through the Odoo adapter (kernel). |
| Odoo bridge `PATCH /codestra/middleware/v1/tasks/{task_id}` answers `{task_id, summary}` without the parent `profile_id` | ODOO_FOLLOW_UP | The kernel reads notes/tasks back through the parent profile's list; `crm.task.update.v1` therefore has no read surface and is recorded as an UNSUPPORTED readback (dead-lettered with the reason, never silently completed). Returning `profile_id` from that route (as note.create/task.create/task.complete already do) makes the readback work without a Middleware change: `OdooAdapter.normalize_result` already persists `task_id:N@profile_id:P` whenever the answer carries it. |
| Klyrow SMTP relay boundary (codestra_klyrow_smtp direct relay) | EMAIL_BOUNDARY_DECISION | Middleware email effects execute only through the klyrow-email adapter behind EMAIL_DELIVERY (off). A direct SMTP relay inside Odoo is outside Middleware's authority; recommended: route through email.message.send.v1. |
| Caddy PR #174 vs prepared integration branch | CADDY_RECONCILIATION | Caddy routes /platform/v1* to Kong and 404s /metrics and /internal/* at the edge, which matches V3 (/metrics private, /internal/v1/* service-only). Re-pin to MIDDLEWARE_V3_CONTRACT_SHA256 after merge. |
| Kong six-route prep (PREPARED_DISABLED, pinned to be25ea3a…) | DECIDED_BY_V3_CONTRACT | Paths, scopes, 202+Location, Idempotency-Key on the three POSTs (commands, cancel, replay), upstream middleware-integration-api:8095 match; re-pin the vendored contract to 9c32daecd4a15104c6f9ff60ce19c8f7e78707fb31d9fd9fcb55b1b8dfa3512b (or the post-merge readback). |
| OpenBao runtime secret resolution for adapters | NOT_RELEVANT to this PR | Adapters receive SecretReferences (#283 model) through AdapterContext; resolution stays inside the worker runtime; no OpenBao change here. |
