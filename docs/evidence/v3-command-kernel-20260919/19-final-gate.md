# 19 — Final gate

BASE_SHA=22d023a9c65b0789a0f7ee6c28548753521a9eff
V3_BRANCH=mission/middleware-v3-command-kernel-20260919
V3_PR=https://github.com/ingtrader21-spec/Middleware-/pull/289
V3_HEAD_AT_EVIDENCE_WRITE=4d1a6d9ea9cb88f0b71defe7c8fbc01509bdb205
(V3_FINAL_SHA / V3_FINAL_TREE / CI / review / merge fields are recorded in the PR and the final report once frozen; the first exact-SHA CI round on 5370ae1 was green on every job except REPOSITORY_GOVERNANCE, which required registering the Postgres kernel suite's integration gate and removing a dynamic skip, and `Test exact repository SHA`, where the four `tests/test_agent_realtime_authorization.py` cases — PostgreSQL-gated, so unreachable on the Windows host — failed with `application settings are unavailable`: the converted `/api/v1/agent/events` handler resolved Redis through `get_settings`, which a router-only app (no `app.state.settings`/`runtime`) cannot satisfy, while the original handler read the process settings. `get_redis_client` now falls back to the process settings; reproduced against the disposable PG16 (4 failed → 10 passed) and covered by `tests/test_core_providers.py`. Both fixed in the follow-up commits; the second CI round on 148df2c was green on every check. Codex review round (five threads on 5370ae1, all verified real and fixed on top of 148df2c): (1) canary compose moved to SCHEMA_HEAD 0067 while the deploy controller still asserted 0059 → controller aligned to `0067_service_catalog_monitoring_state` (the existing `validate_production_runtime_deployment.py` already expected 0067); (2) `workers/run_outbox.py` refused to start when only kernel adapters were enabled → adapter dispatch is a worker mode (`tests/test_run_outbox_modes.py`); (3) notes/tasks readback always UNSUPPORTED → parent-list readback + reconciler dead-letters UNSUPPORTED in one cycle with the reason (conformance + kernel tests); (4) `complete_task` called without the required payload → record always passed; (5) CRM retry with `Idempotency-Key` but no `X-Correlation-ID` → 409 → correlation id derived from (tenant, command type, key) (`test_crm_bridge_routers.py`). Public route contract regenerated byte-identically (digest unchanged, 117 rows); OpenAPI 315 ops unchanged; ROUTE_AUTHORITY=MATCH. Pre-existing, out of scope: `.github/workflows/exact-main-production-release.yml` still asserts alembic head 0059 (its own test pins it) — the production release path is fail-closed on main today, PRODUCTION_GO=NO.)

Local proof (host, Python 3.12 pinned venv; CI on the exact SHA is authoritative): kernel 49, API 11, security matrix 26, conformance 74 (+9 skipped), route shadowing 4, route authority 3, PostgreSQL kernel 9 (disposable PG16) — all green; existing suites touched by the change green (commands, operations, CRM routers, tenants, session context, webphone, telephony, agent realtime, readiness challenge except the 4 Windows file-mode failures that fail identically on clean main, governance, route ownership, contract parity, authority assets/convergence, release manifest, production runtime deployment).
PROVIDER_EFFECTS=0  PRODUCTION_EFFECTS=0  PRODUCTION_GO=NO
