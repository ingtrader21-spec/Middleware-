# Alertmanager → Middleware (2026-09-17) — Codestra-Alertmanager @ `d5da15851cfc`

Routing: `middleware-default` with severity routes `middleware-critical|high|warning|informational` and `middleware-heartbeat` (CodestraWatchdog); group by `alertname service environment severity`; three inhibition rules (critical > high/warning/informational, high > warning/informational, warning > informational) plus deployment-in-progress and OpenBaoSealed inhibitors. Every receiver posts to `url_file: /run/secrets/middleware-alert-webhook-url` with `authorization.credentials_file: /run/secrets/middleware-alert-webhook-token` (Keycloak client `alertmanager`, OpenBao identity `alertmanager`, prefix `observability/alertmanager/`).

Middleware ingestion (`app/observability_alerts.py`, unchanged contract): `POST /v1/integrations/alertmanager/events` and `/status-events` require the `alertmanager` client, `Idempotency-Key` (bounded regex) and `X-Correlation-ID`; native webhook mode `X-Alertmanager-Native-Webhook: v4`; per-alert fingerprint deduplication (a replay returns the same `incident_id` with `duplicate: true`, HTTP 200 vs 202); receiver allow-list; bounded body/alerts; incidents under `/v1/observability/incidents/*` with timeline and notification attempts.

Runtime proof in this stage: the TEST_SYN runner step `alert-ingestion` posts one informational synthetic alert (`alertname=TestSynSyntheticAlert service=test-syn environment=staging business=platform correlation_id=<id>`) **twice** to `POST /api/v2/alerts` and requires exactly one Middleware incident with that `correlation_id` label within the wait window (`test_duplicate_incidents_fail_idempotency` proves the failure path). Collector reader `read_alertmanager` digests `config.original` for reconciliation.

Source validation: routing matrix 11 cases (`scripts/test_stage6_alert_routing.py`, `amtool 0.34.0` check-config PASS at source stage), `validate_codestra_alertmanager.py` incl. secret references PASS. Runtime execution against staging Alertmanager: **not performed**.
