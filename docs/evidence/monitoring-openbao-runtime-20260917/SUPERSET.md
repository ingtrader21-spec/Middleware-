# Superset (2026-09-17) — Superset @ `ec9591c094f3`

Role `business-analytics-only` (`codestra/monitoring-platform-boundary.v1.json`): consumes curated read-only projections from Middleware/Odoo on the reporting schema or replica with a read-only database role; never becomes a Prometheus replacement, log database, trace backend, OpenBao administration interface or operational writer. Forbidden datasource hosts: `prometheus loki loki-query tempo alertmanager openbao bao.codestra.media middleware-integration-api`; forbidden environment variables: `BAO_TOKEN VAULT_TOKEN BAO_ADDR VAULT_ADDR OPENBAO_TOKEN`. Identity `superset-analytics` → `codestra/<env>/analytics/superset/`; secret references vendored; `validate_monitoring_platform_boundary.py` PASS, unittest 4 OK, CI step. Not a component of the reconciliation loop; no runtime proof required beyond the boundary validator and the Blackbox `https://superset.codestra.media/health` GET probe.
