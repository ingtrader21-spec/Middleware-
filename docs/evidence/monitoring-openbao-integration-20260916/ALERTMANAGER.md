# Alertmanager -> Middleware (2026-09-16) — `Codestra-Alertmanager` `d5da1585`, PR #29

- Receivers: six Middleware-only webhook receivers (`url_file` + `credentials_file: /run/secrets/middleware-alert-webhook-token`, `send_resolved: true`); the bearer is the OpenBao reference `codestra/<env>/observability/alertmanager/middleware-webhook` (identity `alertmanager`) declared in `codestra/secret-references.v1.json`.
- Middleware ingestion (`app/observability_alerts.py`): `POST /v1/integrations/alertmanager/events` (native and canonical paths), `POST /v1/integrations/alertmanager/status-events`; fingerprint + idempotency dedup — a replayed delivery returns 200 with the same `incident_id` and `duplicate: true` (`tests/test_observability_alerts.py::test_native_webhook_derives_transport_identity_and_deduplicates`); resolved/firing transitions, suppression reconciliation, source timestamps, tenant/environment isolation and the incident lifecycle (`/v1/observability/incidents/*` acknowledge/resolve/reopen, timeline, notification-attempts) are unchanged existing behaviour.
- Inhibition: `OpenBaoSealed`/`OpenBaoNoActiveLeader` explain dependent OpenBao symptoms per environment (the sealed alert is never inhibited and never triggers an unseal); `CodestraMiddlewareMetricsScrapeFailing` explains the missing TEST_SYN signal.
- Routing certification: Stage 6 matrix 11 staging cases (`DELIVERY_PERFORMED=NO`) including OpenBao critical/warning, ingestion stalled, log pipeline down, TEST_SYN informational -> their severity receivers.
- Checks: `validate_codestra_alertmanager.py` PASS; `amtool 0.34.0 --enable-feature=utf8-strict-mode check-config` SUCCESS (7 inhibit rules, 6 receivers); `test_stage6_alert_routing.py --amtool` PASS; config bundle manifest re-pinned over committed bytes (verified against index blobs).
- Authority split unchanged: Prometheus rules; Alertmanager grouping/inhibition/silence/routing; Middleware durable incidents, audit, delivery intent. No alert rule is duplicated inside Middleware.
