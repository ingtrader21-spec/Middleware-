# Tempo (2026-09-16) — `Codestra-Tempo` `911218af`, PR #33

- Receivers: OTLP gRPC/HTTP now terminate TLS 1.2+ with `/run/secrets/tempo_server_cert|key` (OpenBao reference `observability/tempo/receiver-tls`), closing the handshake gap with the gateway exporter (`tls.insecure=false`, CA, `server_name_override`, `X-Scope-OrgID`); `TEMPO_GATEWAY_TLS_CROSS_CHECK=PASS` against Telemetry `bf48a9c1`.
- Received/discarded spans never logged; request headers never logged/traced; attributes bounded at 4096 bytes; tenants are business domains; multitenancy enabled; usage reporting off.
- Trace-propagation contract `codestra/trace-propagation-contract.v1.json`: W3C `traceparent`/`tracestate`, `X-Correlation-ID -> correlation.id` (<= 128, opaque), required span attributes (`service.name`, `service.version`, `deployment.id`, `codestra.application`, `codestra.business`, `deployment.environment.name`, `correlation.id`), forbidden attributes, hops Caddy -> Kong -> Middleware -> Odoo/N8N (declared `planned`; instrumentation is owned by those repositories), TraceQL proof `{ span.correlation.id = "<TEST_SYN correlation id>" }`, incident join via the Middleware timeline.
- Validator repair: the enterprise-profile validator asserted a legacy `encoding: zstd` field that vParquet4 no longer accepts (removed in b274304a) and had been failing on `development`; it now asserts the locked block version. All Tempo validators PASS; tests 9/9.
