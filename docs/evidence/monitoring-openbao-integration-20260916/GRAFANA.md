# Grafana (2026-09-16) — `Codestra-Grafana-` `9c119fe8`, PR #32

- Datasources: Prometheus, Loki (derived fields TraceID/CorrelationID), Tempo (traces-to-logs/metrics, service map), Alertmanager (read-only) and **Codestra Middleware Observability** (`yesoreyeram-infinity-datasource` 4.0.0, package sha256 `2ebce2b2937983651d64991e833e48294b8002581a105741ae3df967e329905d` pinned in `codestra/release/plugin-lock.json` and verified in the image build; `allowedHosts` = `http://middleware-integration-api:8095`; bearer `$__file{/run/secrets/grafana_middleware_observability_token}` = OpenBao reference `observability/grafana/middleware-observability-token`; `oauthPassThru: false`; GET only).
- Dashboards (53 generated): existing platform/business/incident/security/database/SLO/environment/server/deployment views plus **OpenBao Secrets Authority — Operational Status** (initialised, sealed, leader/standby, health probe, request rate/latency, audit success/failure, token and lease counts, storage latency, process CPU/memory, filesystem, restarts, audit-stream authentication failures, policy denials, root-token activity, policy mutations — no secret path, token value, unseal or recovery key) and **Middleware Operational Control Plane** (open incidents, catalog monitoring state, per-component reconciliation, secret-reference health, ingestion/dedup counters, monitoring-readonly scrape, Alertmanager delivery failures).
- Secrets: every `$__file{/run/secrets/...}` in `grafana.ini` and the datasource provisioning is an OpenBao reference (identity `grafana-runtime`); the validator rejects any `secureJsonData` value that is not such an expansion (negative control verified), an unlocked/unverified plugin (negative control verified) and unsigned-plugin loading.
- `validate_codestra_observability.py` PASS.
