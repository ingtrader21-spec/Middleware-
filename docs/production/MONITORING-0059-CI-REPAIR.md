# PR 226 CI repair and protected trust update

PR 226 contains campaign migration `0058_campaign_design`, then monitoring migration `0059_integrated_monitoring`. Its source authority already requires 0059. The protected-base trust launcher still admits only the older convergence validator bytes.

## Repaired source

The schema verifier now derives campaign and monitoring tables separately and admits only the corresponding literal name prefix. The physical catalog contract includes the three monitoring tables and the exact approved migration-history hash. Their signatures were generated by applying the actual 0059 upgrade in an isolated PostgreSQL 17.6 container with no network, published ports or host data mounts. The container was removed afterward; production databases were not changed.

All existing 40 schema signatures were preserved. Database regressions now reject missing monitoring tables, missing replay uniqueness, missing scope indexes and weakened tenant nullability. Shared OpenAPI and completion-matrix artifacts were regenerated from the runtime. Deployment source and response fixtures consistently require 0059, preserving the historical migrations and signed observations at their original revisions.

Validation: the complete local suite passed 2,783 tests and 94 subtests, with 138 environment-gated skips. Ruff and the generated 36-operation monitoring contract passed. Real database corruption regressions are committed for the disposable PostgreSQL CI job; the local full-suite result does not claim those skipped integration tests ran.

## Why the two trust checks fail

Both `orchestrator-contract` and `trusted-orchestrator-evidence` stop at:

`mutating job lacks disable marker: .github/workflows/middleware-ci.yml:source-head-validation`

The CI job invokes `scripts/run_ci.sh`, which invokes the checksum-pinned `scripts/validate_middleware_authority_convergence.py`. The campaign/monitoring integration updated its schema requirement. Its checksum therefore differs from the old trusted pin. The classifier treats changed pinned executable bytes as unapproved, before deciding whether their operations are read-only.

[The exact pin patch](evidence/monitoring-authority-validator-pin.patch) and [the hash manifest](evidence/monitoring-authority-trust-transition.json) are review artifacts. They are not applied to active policy.

| Object | SHA-256 |
| --- | --- |
| Current convergence pin | `23679aee112625c778d7607187ea505ca2f9f0983411143728d785d011298ae4` |
| Convergence source in approved PR head eb37a6d | `07c3a1bd8780de9cf3d2f04f441c4d1423a781b2a4d0f5d24d0bd792b7daa1c7` |
| Current orchestrator validator | `4be8302e9c383b1da8a9afc7e25c371910ae963ed9a40d5bcf9a4091ab818d76` |
| Proposed orchestrator after only that pin update | `dd932416dac9fd0fe8c9e2a03a7c20ab4eb0ace1bc8d4d72c57d9818055a28a6` |

Offline simulation of that one pin update passes the complete orchestrator contract and its existing negative regressions. This simulation is not protected CI or approval.

## Required protected maintenance

The security owner must authorize the exact proposed orchestrator generation through the protected-main trust process, then apply the reviewed pin patch to the candidate and rerun both trust checks. The current launcher intentionally rejects candidate changes to itself or the trusted evidence workflow. An ordinary approval on the application PR does not modify that policy.

Keep the existing release-validator digest, exact-source check, independent ownership, one-way validator transitions, deny-by-default release intent and runtime-disable conditions. Do not add `continue-on-error`, disable validation jobs, weaken the classifier or self-admit a new validator from candidate source. No active trust pin, launcher or protection was changed by this repair.
