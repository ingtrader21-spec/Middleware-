# Codestra Middleware Phase 1

Transactional VICIdial ingestion and outbox schema. Only `TEST_SYN` is accepted. The Alembic migration is intentionally not applied by this change; apply it only after a separately approved database change window.

All Odoo and n8n adapters are disabled and no external service is contacted.
